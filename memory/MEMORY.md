# Memory Index

- [DB Schema](project_db_schema.md) — postgres `markets` db (`01_schema.sql`+`02_seed.sql`): normalized `currencies` lookup, `markets` precision cols, `exchange_markets` `*_rebase` cols (feed raw-pipeline job 3); ⚠ local seed stale vs server DB (server has 8 exchanges incl. ramzinex/bitget/bybit/ompfinex/okx, subscribed rows)
- [Kafka Topic Strategy](project_kafka_topic_strategy.md) — ID-based topics: raw ingest `ex{exchange_id}-raw` (7d), per-exchange+pair stages `ex{id}-p{id}-{stage}-flink` (1h), dead-letter `ex{id}-p{id}-rejected-flink` (7d), aggregated output `p{pair_id}-{side}` (6h), key=null, all pre-provisioned by warmup.sh; **⚠ there is NO `ex{exchange_id}-p{pair_id}-{side}` input topic — that naming was wrong here until 2026-08-10 and had already leaked into the staleness exporter, see [[staleness-exporter]]**; **normalizer stage topics `ex{id}-p{id}-{stage}-flink` (2026-07-19) created FIRST in warmup.sh because sources read `latest`, shared `rejected-flink` dead-letter at 7d**;
- [Avro Schemas](project_avro_schema.md) — canonical schemas/subjects: `aggregated-order-book-event` TRUE Confluent Avro wire format for the terminal aggregator + web; raw-pipeline trio `raw-order-book-event` (nullable asks/bids for per-side snapshots, nullable sequence*id, jump 0 = snapshot feed) + `order-book-snapshot` + `rejected-order-book-event`, all registered via warmup.sh; display-only fields rule; price/qty as strings; \*\*per-step latency timings (2026-07-15): ONE `pipeline_timings: PipelineTimings` record (named `{stage}*{in,out}`nullable millis, NOT an array) on all raw-pipeline schemas; total end-to-end =`book_build_out − event_time`\*\*
- [Order Book Aggregation](project_orderbook_aggregation.md) — business rules: union-never-sum across exchanges, sort asks↑/bids↓ tie-break qty desc, snapshot/update semantics, strict `sequence_id`/`sequence_jump` gap handling (reused by raw-pipeline job 2); no Flink checkpointing configured (cold-start deferred)
- [Order Book Web UI](project_orderbook_web.md) — standalone Go app (`web/`, hexagonal `internal/` packages) consuming `p{pair_id}-{side}` Avro topics, id→display enrichment from postgres, live WebSocket order book; vendored deps, dockerized
- [Normalizer Scaffold](project_normalizer_scaffold.md) — M1 DONE 2026-07-15: `flink/normalizer/` Maven multi-module (`normalizer-parent` + `normalizer-common`, job modules added per-milestone); common = models/serdes/AvroSchemaLoader/RefreshingLookup/Decimals (18 tests); parameterized run-job.sh (manifest Main-Class); `docker-compose.yml` = full stack, one 8-slot Flink cluster for the 5 upstream normalizer jobs + the terminal aggregator; **normalizer SMOKE RULE (2026-07-15): feed RAW to `ex{id}-raw` only, run the whole chain, capture the job-under-test's topic, event_time = execution time so per-stage pipeline_timings are verified live**
- [Pair Extractor](project_pair_extractor.md) — M2 DONE 2026-07-15: `job-pair-extractor` (raw pipeline job 1) — 7 parsers (ex1–6+8), ex7 = drop-with-counter (scope lives in `Parsers.byExchangeId()`); **ex1 nobitex REVISED 2026-07-21 — NOT snapshot-only: NiFi publishes a REST snapshot (`action:"snapshot"`+injected `pair` field → type snapshot/null seq) AND WS deltas (Centrifugo, no action → type update/`seq=pub.offset`/jump 1); coupled job-2 resync + deploy-together warning, see [[type-validator]]**; **ex2 bitpin REVISED 2026-07-25 — the identical split (REST `action`+injected `pair`=`BTC_USDT`; WS `orderbook:{market}` → update/jump 1); ⚠ `event_time` has TWO wire types in ex2 — REST = epoch-millis NUMBER, WS = ISO-8601 STRING, same field name; NO job-2 change (ex1's `baselinePending`+`out_of_order` guard are exchange-agnostic); ex1/ex2 REST payloads are now identical except the timestamp field NAME (`lastUpdate` vs `event_time`), so neither snapshot branch may key off `action` alone; unit-verified, not live**; **ex9 lbank seeded by a teammate 2026-07-20 but NOT implemented — same silent drop-with-counter, needs wire samples + an `LbankParser`, todo.md M9**; `latest` offsets, per-exchange event_time stamping (ex3/ex4 = processing time), wire level order passed through, postgres driver shaded into job jar; repeatable e2e smoke test `smoke-pair-extractor.sh` (all 10 fixtures green, deterministic via pre-produce end-offset read); 2 runtime gotchas fixed 2026-07-15 → `ExchangeMarketsLoader` needs explicit `Class.forName` postgres driver load (Flink child-first classloader vs DriverManager), and registry `raw-order-book-event` had to be re-registered to v2 with `pipeline_timings` or the Avro sink NPEs
- [Type Validator](project_type_validator.md) — M3 DONE 2026-07-15: `job-type-validator` (raw pipeline job 2) — keyed `(exchange_id,pair_id)` `KeyedProcessFunction` `{lastSeq, awaitingSnapshot, baselinePending}`; **2026-07-21 ex1 resync: a null-seq snapshot (ex1 REST / ex3) sets `baselinePending`, the first `update` after it adopts its offset as the baseline — exchange-agnostic, ex6/ex8 `no_baseline` unchanged**; discriminator is `type`+`sequence_id==null` NOT jump alone (delta snapshots also carry jump>0); **null-seq frames (ex3, ex1 REST) have no seq → ordered by EVENT TIME: an older-than-last-accepted one rejects `out_of_order` before re-arming baseline (2026-07-21 fix — old REST snapshot replayed after WS deltas must not overwrite the newer book)**; snapshot = out-of-order drop, update = gap/jump rule (`no_baseline`/`awaiting_snapshot`/`stale_or_duplicate`/`sequence_gap`); valid → `-type-validated-raw-flink`, rejects → dead-letter `-rejected-flink` side output; **2026-07-21 gap-drop: true-gap branch ALSO emits a synthetic `type="reset"` on the main stream (still dead-letters the update) → job 5 empties the book → aggregator drops the exchange**; **LIVE BUG 2026-07-22: `type` is an Avro ENUM not a free string — the `"reset"` symbol NPE'd the serializer, fixed by adding it to `Type` in `raw_order_book_event.avsc` + re-registering (v2/id 7); resubmit jobs to re-fetch; gap-drop is exchange-agnostic → redo only the LIVE test per delta feed ex1/ex6/ex8**; 11 harness tests + smoke 3/3; had to re-register stale `rejected-order-book-event` to v2 (pipeline_timings NPE, same as job 1); smoke gotchas = avro producer `use.logical.type.converters`, newly-discovered topic seeks earliest
- [Rebaser](project_rebaser.md) — M4 DONE 2026-07-18: `job-rebaser` (raw pipeline job 3) — stateless UNKEYED `ProcessFunction`, `price × 10^price_amount_rebase` / `quantity × 10^volume_amount_rebase` exact via `scaleByPowerOfTen`; exponents from `RefreshingLookup` keyed `{exchange_id}|{market_id}` (own `RebaseFactorsLoader`, NOT job 1's market-string loader); **missing row → dead-letter `no_rebase_row` (user decision — this is why it's a ProcessFunction not a RichMapFunction)**; null side stays null (ex3 half-book); smoke temporarily UPDATEs the rebase row + waits out the lookup refresh + restores in an EXIT trap (seeded row is 0/0 = identity, proves nothing); 9 tests + smoke 3/3
- [Precision](project_precision.md) — M5 DONE 2026-07-18: `job-precision` (raw pipeline job 4) — stateless UNKEYED `RichMapFunction`, truncates DOWN to `markets.{price,quantity}_precision` via `RefreshingLookup` keyed by `pair_id` ALONE (precision is a property of the market, not the exchange); **nonzero quantity truncating to 0 → EMIT "0" and KEEP the level (user decision, revising an earlier drop decision) — job 5 reads that as a level-delete, which is intended: size below the lot precision is not representable liquidity; flooring was rejected as inventing size**; **2026-07-20 FIX — prices that COLLIDE after truncation (1.234 and 1.235 both → 1.23) are MERGED into one level with their quantities SUMMED, raw sum truncated once (not truncate-then-sum), same rule on snapshot and update; previously both were emitted and job 5's price-keyed MapState silently kept only the last, losing liquidity — so level count in NO LONGER equals level count out**; **NO dead-letter — null/missing precision is a passthrough, deliberately unlike job 3** (un-truncated is correct-but-verbose, un-rebased is silently corrupt); nullable cols need `rs.getObject` not `rs.getInt`; 21 tests green, smoke extended for the merge but NOT re-run live since (no DB mutation — seeded 2/8 is already non-identity)
- [Book Builder](project_book_builder.md) — M6 DONE 2026-07-18: `job-book-builder` (raw pipeline job 5) — keyed `(exchange_id,pair_id)` `KeyedProcessFunction`, one `MapState` per side (canonicalized price → quantity), emits the FULL book (`order-book-snapshot`) on every event; **snapshot vs update differ ONLY by "clear the side first"**, so `quantity == 0` is a delete in both — and those deletes also come from job 4's dust truncation, not only from exchanges (commented at the delete branch); **`null` side ≠ empty side — THE ex3/wallex per-side snapshot merge point**; sides sorted asks↑/bids↓ because MapState order is undefined; no seq re-validation, no dead-letter (job 2 already validated); cold-start = empty book until the next snapshot (no checkpointing, platform-wide gap); **registered `order-book-snapshot` was stale v1 without `pipeline_timings` — re-registered, same trap as jobs 1/2**; 15 tests + smoke 3/3 (3 ordered cases on ONE accumulating book)
- [Aggregator](project_aggregator.md) — terminal `job-aggregator` (raw pipeline job 6) consuming job 5's full books directly, unioning across exchanges per `(pair_id,side)` → frozen `p{id}-{side}`, subject **`aggregated-order-book-event`** (TRUE Confluent Avro; web decoder resolves by wire-id, maps by field name); consumes job 5's `order-book-snapshot` full books directly so a `type="reset"` ([[type-validator]]) that empties an exchange's book drops it from the union; `SnapshotSplitter` → `CrossExchangeAggregator` (union-never-sum, empty book ⇒ exchange drops out) → inline sink; models/serializer `AggregatedOrderBook`/`AggregatedLevel`/`AggregatedOrderBookSerializer`; deployed via Makefile `refresh-normalizer` (submitted downstream-first; stateful, so resetting it means recycling the job); ([[type-validator]] reset marker + [[book-builder]] reset branch feed it); 12 tests green; **`smoke-aggregator.sh` (2026-07-22) — raw ex8→whole chain→p1-{side}, asserts ex8 by exchange_id, 4 cases incl. gap⇒reset⇒ex8 drops⇒resync returns; syntax-only, not run live**
- [Raw Pipeline Decision](project_raw_pipeline_decision.md) — FINAL 2026-07-13: NiFi publishes verbatim raw payloads to `ex{id}-raw`; 6 chained Flink jobs (pair-extract → type-validate → rebase → precision → book-build → aggregator) in ONE Maven multi-module project; accuracy > latency; raw samples RESET then RE-CAPTURED 2026-07-14 for ALL in-scope exchanges into `sample-raw-data.md` (3 regimes confirmed; job-2: gap/jump rules on delta feeds ex6 bybit `u`/jump-1 + ex8 okx `ts`/jump-300 — **plus ex1 (2026-07-21) and ex2 (2026-07-25), both re-classified from snapshot-only to REST-snapshot+WS-delta, jump 1**; remaining snapshot feeds ex4 `pub.offset` + ex5 `seq` get an OUT-OF-ORDER drop check only — ex3 wallex has no ordering field, no protection; non-book frames just discarded; ex1 always single-doc; remaining lead: bybit qty-"0" delete frame); **ex7/ompfinex POSTPONED — scope is ex1–ex6 + ex8**; **per-step latency tracking REQUIRED 2026-07-15 (named `PipelineTimings` record, ingest+emit per job, total = book_build_out − event_time)**; tasks in todo.md
- [BigDecimal Rules](project_bigdecimal_rules.md) — prices/quantities are `BigDecimal`-from-wire-string, never double; scale-sensitive equals → TreeMap or canonicalized string keys
- [TDD Workflow](project_tdd_workflow.md) — team is test-first; existing code gets spec-based (not characterization) coverage; Flink operator test harness stack; JaCoCo-on-JDK26 noise is non-fatal
- [E2E Harness](project_e2e_harness.md) — `e2e/`; every scenario asserts job 5's snapshot stream AND job 6's final aggregated book (all 41 now spell `WantAggregated` out; a nil one is derived from the last wanted snapshot, which only holds while a scenario feeds ONE exchange); also the `-serve` HTTP mode (`POST /scenarios/run`, chi, one run at a time, failed scenario = 200 + `"status":"failed"`) and its swaggo docs (`/swagger/`, build `e2e/` with `-mod=mod` — never `go mod vendor`, regenerate `e2e/docs/` with `swag init` after struct changes; the Try-it-out example is injected at serve time from `scenario.Ex1PrecisionDust`, not in the committed spec); **logging is `log/slog` since 2026-08-08 (`-log-level`, default info) — Info = scenario start/PASS/FAIL + process milestones, Debug = everything per-scenario; the real noise was never the log lines but the per-scenario `mvn` and the `docker compose` output, which now buffer and replay only on failure**
- [Simulation Flag](project_simulation_flag.md) — `simulation` int added 2026-08-03 across all 4 schemas + all 6 jobs + e2e + web; 0/absent = live, 1 = simulation, other values undefined and carried verbatim; **TWO wire carriers — a root FIELD for the six object-root exchanges, a TRAILING THIRD ARRAY ELEMENT `{"simulation":N}` for ex3/wallex** (whose root is an array; 2-element frames still parse as 0, 4+ dropped); **per-LEVEL on the aggregated topics** (`exchange_id, simulation, price, quantity`) because job 6 unions across exchanges; job 2's synthetic `reset` marker explicitly inherits it, job 5 stamps last-event-wins and keeps it OUT of MapState; ⚠ re-register all 4 subjects or the Avro sink throws (same trap as `pipeline_timings`); every e2e example is `simulation: 1`; web plumbs it to the WebSocket but does NOT render it
- [Record Lineage](project_record_lineage.md) — `id`/`source_ids` added 2026-08-03 across all 4 schemas + all 6 jobs + e2e + web (**field was `sink_id` until the 2026-08-04 rename — pure rename, same logic**); **`id` is RE-MINTED at every hop, it is NOT an end-to-end correlation id despite the name** (7 different ids across the pipeline; walk the chain hop by hop) and `source_ids` is IMMEDIATE parents only; same two wire carriers as [[simulation-flag]] (root field / ex3's trailing object at **index 2**, never a 4th element); **⚠ job 1 DROPS a payload with no id → deploy NiFi's change BEFORE the jars or a feed loses 100% of its data (`dropped-no-id`)**; job 5 is the only fan-in (every event still holding a resting level + always the triggering one → MapState value became `RestingLevel{price,quantity,id}`); **2026-08-08 job 5's snapshot levels each carry a per-level `source_id` = the job-4 event that last SET that price — without it a trace of ONE price dead-ended at "one of these N events", since a record-level set has no mapping back to prices — and the snapshot's record-level `source_ids` array was REPLACED by a single `trigger_id` (it had become `{trigger} ∪ {level owners}`, so it added only the trigger, and only positionally); ⚠ `trigger_id` is NOT always among the level ids — a delete-only event owns none and a reset empties the book — which is exactly why it is its own field; job 6 deliberately does NOT propagate it (a level names the snapshot, the snapshot's level of that price names the event — each hop one step); the snapshot `PriceLevel` is now the ONLY one with the field and `serde/PriceLevels` decides from the SCHEMA, and `order-book-snapshot` is stale AGAIN**; **2026-08-08 e2e `events` realigned field-for-field to the schemas — `last_sequence_id` + `AggregatedSide.event_time` were missing, and the timings model was goavro-WIRE-shaped and wrong 3 ways (union branches keyed by FULL name; logical-type unions keyed `long.timestamp-millis`; value is a number) — see [[e2e-harness]]; all 4 `_example.json` are hand-maintained docs nothing validates, verified 2026-08-08**; job 6 = per-record `id` + per-LEVEL `source_id` (no record-level array); jobs 3/4 are no longer no-ops unlike simulation; dead-letter envelope and its nested event have SEPARATE lineages; ⚠ re-register all 4 subjects; ⚠ Avro returns `Utf8` not `String`; **e2e asymmetry — the 177 SOURCE payloads each spell a literal `id` out (2026-08-04) since they are inputs, and `stampID` now only FILLS ONE IN rather than overwriting (blank id = hard error, duplicate-key trap); every id further down is job-minted and random per run, so e2e checks those structurally then strips them**
- [Scope Discipline](project_scope_discipline.md) — **HARD RULE, user 2026-07-27: do ONLY what was asked, and CONFIRM any extra work before doing it.** "Initialize X" ≠ X plus the first task that uses X; a `todo.md` milestone is a backlog, not permission to implement it; "we will provide Y" means WAIT for Y, don't reconstruct it. Written after a session's unrequested M13-phase-1 code was thrown away
- [Staleness Exporter](project_staleness_exporter.md) — `lpa-staleness-exporter/` derives its watch list from `exchange_markets` and **must mirror `scripts/warmup.sh` names or it reports phantom topics as `stale=1`**; `NORMALIZER_STAGES` is duplicated in both files with nothing enforcing it; dead-letter deliberately unmonitored (inverted semantics); serial poll loop has a known scaling ceiling; **`topic_source: both|db|config` picks the source (`db` ignores the manual `topics:` block entirely) and 5 staleness-*episode* series (stale-since / last-recovery / last-duration + 2 counters) are in-memory and accurate only to ±1 poll interval**; **episodes ALSO persist to `topic_staleness_episodes` (2026-08-11, self-installing table absent from `01_schema.sql`, written in EVERY `topic_source` mode) under 3 sharp invariants — `extract(epoch)::float8` or PG18 hands back a `Decimal` that silently breaks recovery, a UNIQUE partial index because two open rows is unrecoverable, and dropping a watched topic must close its episode**; metric consumers (`KafkaTopicStale`) live outside this repo, except the hand-imported drift-prone dashboard copy `grafana-dashboard.json`
- [NiFi over HTTPS](project_nifi_https.md) — NiFi HTTPS single-user on 8443; raw-IP access fails `400 Invalid SNI` → fix via `NIFI_WEB_PROXY_HOST`
- [Server Build Env](project_ubuntu_server_env.md) — deploy server needed Temurin 21 tarball + apt maven (`scripts/install-deps.sh`); repo root-owned so use `sudo make refresh-normalizer`
