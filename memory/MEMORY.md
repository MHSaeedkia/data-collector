# Memory Index

- [DB Schema](project_db_schema.md) — postgres `markets` db: normalized `currencies` lookup + `markets.base_id`/`quote_id` FKs (was base/quote VARCHAR); 3 exchanges (wallex added), 81 markets, all exchange_markets seeded `unsubscribe`
- [Kafka Topic Strategy](project_kafka_topic_strategy.md) — ID-based topics: input `{side}-p{pair_id}-ex{exchange_id}`, output `{side}-p{pair_id}`, key=null; pre-provisioned; migration COMPLETE (warmup+schema+Flink+web); input retention 1h / output retention 6h (2026-07-11), existing server topics need manual `kafka-configs --alter`; `scripts/warmup.sh` topic creation is sequential (`xargs -P` parallelization attempted 2026-07-11, reverted same day, commit `81c18de`); per-topic `<topic>-value` schema registration tried 2026-07-12, reverted same day (cluttered registry with duplicate subjects) — only the 3 canonical fixed-name subjects (`orderbook-event`/`price-level-event`/`consolidated-order-book-event`) exist; kafka-ui made to offer AVRO per topic WITHOUT registry clutter via custom serde config (`topicValuesPattern` + `properties.schemaNameTemplate`) in the `kafka-ui` service of `docker-compose-orderbook-consolidator.yml`, verified live end-to-end (produced+decoded a real message)
- [Avro Schemas: OrderBookEvent/PriceLevelEvent/ConsolidatedOrderBookEvent](project_avro_schema.md) — `orderbook_event.avsc` (identity `pair_id`/`exchange_id`, display-only `base`/`quote`/`exchange_name`, orderbook-job still JSON-only); `price_level_event.avsc`/`consolidated_order_book_event.avsc` now TRUE Avro wire format for orderbook-consolidator (refactored 2026-07-11, JSON/Jackson fully removed) — ⚠️ web/ ingest still expects JSON and NiFi's producer format is unverified, see [[orderbook-consolidator-decision]]
- [Order Book Aggregation](project_orderbook_aggregation.md) — Generate consolidated order book per pair+side by merging all exchanges; stateful step on top of the transport-level regex merge. Phase 1 done; Phase 3 done 2026-06-29 (merger honours `type`: snapshot=replace, update qty>0 upsert / qty==0 delete; `ExchangeBook` state per exchange_id, price levels keyed by `BigDecimal` in a `TreeMap` so equal prices collapse regardless of NiFi formatting; `sequence_id` drops stale/dups; `sequence_jump` carries expected per-msg delta (increments NOT +1, exchanges jump ~300) — gap handling DONE 2026-06-29: snapshot=unconditional baseline, update in-order iff `seq==lastSeq+sequence_jump`, gap→clear book + `awaitingSnapshot` until next snapshot; cold-start-on-restart DEFERRED)
- [Order Book Web UI](project_orderbook_web.md) — Standalone Go app (net/http + gorilla/websocket + franz-go + pgx) consuming consolidated `{side}-p{pair_id}` topics, resolving ids→display from postgres, rendering a live order book (`web/`); embedded UI, vendored deps, dockerized as `web` service. Refactored 2026-07-06 from one `main.go` into hexagonal `internal/` packages (domain/ports/registry/hub/ingest/kafka/postgres) + testify unit tests; `main.go` stays a thin composition root in `web/` root, Docker/README build commands unchanged. 2026-07-08: price/qty columns zero-padded client-side to the max decimal count seen across the current snapshot (string-based, exactness preserved)
- [BigDecimal Rules](project_bigdecimal_rules.md) — prices/quantities are `BigDecimal` built from the wire string, never `double` (`new BigDecimal("0.1")` exact; `0.1+0.2` float bug; `new BigDecimal(double)` and any `parseDouble` forbidden); add/sub/mul/compareTo exact, division needs RoundingMode; `equals`/`hashCode` scale-sensitive → key levels by `TreeMap`/compareTo not `HashMap`
- [Fake Data Generator](project_fake_data_generator.md) — Go dev tool (segmentio/kafka-go) that publishes synthetic per-exchange input events to `{side}-p{pair_id}-ex{exchange_id}`, standing in for NiFi; hardcoded pairs/exchanges
- [NiFi over HTTPS](project_nifi_https.md) — NiFi runs HTTPS single-user on 8443 (stock apache/nifi 2.x, no custom entrypoint); raw-IP access fails with `400 Invalid SNI` → fix via `NIFI_WEB_PROXY_HOST`
- [TDD Workflow](project_tdd_workflow.md) — team is test-first for all new code; existing Flink `orderbook-job` gets spec-based coverage (assert the Javadoc contract, deviations = bugs not characterization); test stack = JUnit5 + AssertJ + Flink `KeyedOneInputStreamOperatorTestHarness` + JaCoCo
- [Server Build Env](project_ubuntu_server_env.md) — deploy server had no Java/Maven at all; installed Temurin 21 tarball (pom needs java 21, apt only has 17) + apt maven; repo root-owned so use `sudo make refresh-consolidator`
- [Order Book Consolidator Decision](project_orderbook_consolidator_decision.md) — FINAL decision: build the greenfield `flink/orderbook-consolidator/` as a **Java Flink DataStream job**. **THIS-PHASE SCOPE = R1, R2, R4, R5, R6; R3 (per-(exchange,pair) `stale_time`/TTL expiry) is POSTPONED** (deferred work kept in the doc's "Postponed — R3" block + todo). Input = high-volume flat one-level price stream (`{side}-p{pair_id}-ex{exchange_id}`); output unchanged = one consolidated book per pair-side `{side}-p{pair_id}` (web/ untouched). **Two-stage topology forced by keying (HARD RULE, 2026-07-04): stage-1 `KeyedProcessFunction` keyed `(pair,exchange,side)` + `MapState<price,StoredLevel>` does R1 upsert-latest-per-price / R2 qty=0 remove and emits each exchange's book; stage-2 re-keyed `(pair,side)` + `MapState<exchange_id,levels>` does R4 union (never sum equal prices) / R5 sort asks↑ bids↓ tie-break qty desc `BigDecimal`-from-string ([[bigdecimal-rules]]) / emit.** R6 single `KafkaSink` per-record topic routing. TDD via operator test harnesses ([[tdd-workflow]]). **BUILDING IN PROGRESS**: Step 0/0.5 done (schemas `price_level_event.avsc`+example, registered via warmup.sh); Step 1 done 2026-07-04 (`flink/orderbook-consolidator/pom.xml`, base pkg `io.tibobit.consolidator`, mainClass `OrderBookConsolidatorJob`, no Postgres dep; each flink project fully self-contained — `flink/orderbook-job/` + `flink/orderbook-consolidator/` each hold own `run-job.sh`/`Makefile`/`Dockerfile`/`confluent-deps-pom.xml` (consolidator Dockerfile drops Postgres); per-project root composes `docker-compose-orderbook-job.yml` + `docker-compose-orderbook-consolidator.yml`; shared `flink/Makefile`+`flink/Dockerfile`+`flink/confluent-deps-pom.xml`+root `docker-compose.yml` removed; run ONE stack at a time (identical container_names/ports)); Step 2 done 2026-07-04 (data model in `io.tibobit.consolidator.model`: `PriceLevelEvent` input POJO (Jackson snake_case, ignore-unknown — matches `price_level_event.avsc`), `StoredLevel` stage-1 state (quantity+eventTime, plain POJO), `ExchangeBook` stage-1→stage-2 record (`levels:List<ConsolidatedLevel>` — consolidator has ONE rung type, so `PriceLevel` NOT copied; distinct from old orderbook-job ExchangeBook), `ConsolidatedLevel`+`ConsolidatedOrderBook` copied verbatim as the fixed web-UI wire shape; `mvn compile` green); Step 3+4 done 2026-07-04 (`PriceLevelEventDeserializer` in `io.tibobit.consolidator.deserializer` mirrors orderbook-job's deserializer — lazy transient ObjectMapper; `PriceLevelSourceFactory.create(bootstrap,groupId)` in `io.tibobit.consolidator.source` = ONE `KafkaSource` over ALL input topics via pattern `(asks|bids)-p[0-9]+-ex[0-9]+` @ `latest` offsets — DECISION: single source, route by event's own pair/exchange/side through `keyBy` (no per-pair source, no PairsLoader/Postgres, new pairs/exchanges auto-picked-up); the `-ex{n}` segment excludes output topics `{side}-p{pair_id}` so the job never re-consumes its own output; `mvn compile` green); Step 5+6+7 done 2026-07-04 (operators in `io.tibobit.consolidator.operator`: **stage-1 `PerExchangeBookBuilder`** keyed `(pair,exchange,side)` `MapState<price,StoredLevel>` — R1 upsert-latest iff `event_time>=stored` else drop-stale-no-emit, R2 `quantity signum()==0` remove (qty=0 on absent price = no-emit), emits `ExchangeBook` w/ `event_time`=max-of-levels (or trigger event's time when emptied); **price MapState key is CANONICALIZED** `new BigDecimal(price).stripTrailingZeros().toPlainString()` because MapState is hash-based & won't collapse scale like the old `TreeMap` ([[bigdecimal-rules]]); **stage-2 `CrossExchangeConsolidator`** keyed `(pair,side)` `MapState<exchange_id,ExchangeBook>` — R4 straight-concat union never-summed, R5 sort asks↑/bids↓ tie-break qty desc, `event_time`=max-across-exchanges, emits `ConsolidatedOrderBook`; sort direction chosen **per-record from `book.getSide()`** (both comparators built in `open()`) NOT constructor — one operator instance serves both sides in the unified topology. Step 6 `ConsolidatedOrderBookSinkFactory.create(bootstrap)` = single `KafkaSink` w/ `setTopicSelector` → `{side}-p{pair_id}` per record (R6), `ConsolidatedOrderBookSerializer` copied into `io.tibobit.consolidator.serializer`. Step 7 `OrderBookConsolidatorJob.main`: source→keyBy(pair,exchange,side)→stage1→keyBy(pair,side)→stage2→sink(+print), `noWatermarks`, group `orderbook-consolidator-flink`; both `keyBy`s use **anonymous `KeySelector` classes NOT lambdas** (Flink can't infer String key type from a concat lambda). `mvn compile` green. Step 9 done 2026-07-04 (24 spec-based tests green via `KeyedOneInputStreamOperatorTestHarness`: `PerExchangeBookBuilderTest` 9 / `CrossExchangeConsolidatorTest` 9 / `PriceLevelEventDeserializerTest` 6; JaCoCo 0.8.12-on-JDK26 prints non-fatal bootstrap-instrumentation noise but `mvn test` exits 0 — see [[tdd-workflow]]). Remaining: Step 8 build+deploy. 2026-07-08: jobmanager/taskmanager got log-dir volumes in the compose file; schema-registry/kafka-ui/web intentionally left without (stateless/no local persisted data); 2026-07-08: found+named all 9 server anonymous volumes from base-image VOLUME instructions (nifi x6, schema-registry, postgres, kafka secrets) in the compose file — existing anonymous volumes on server left untouched, user to decide migration separately; 2026-07-08: schema-registry crashed on cold start on a Kafka-healthy-but-`_schemas`-leaderless race, fixed live + added `restart: on-failure` to all 7 services (commit 1d08353), not yet deployed to server); 2026-07-11: refactored source+sink JSON/Jackson → true Confluent Avro binary — ⚠️ NOT deploy-safe yet: `web/` ingest still does `json.Unmarshal` on the output topics, NiFi's actual input-topic producer format is unverified; 2026-07-12: schema source of truth moved to registry-only at runtime — `AvroSchemaLoader.loadLatest(url,subject)` fetches the schema live from Schema Registry (subjects `price-level-event`/`consolidated-order-book-event`) instead of a bundled `.avsc` copy; the `maven-resources-plugin` copy step was retargeted to test resources only, shaded jar no longer contains any `.avsc` file (verified via `jar tf`), 25/25 tests still green
