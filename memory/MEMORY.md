# Memory Index

- [DB Schema](project_db_schema.md) — postgres `markets` db (`01_schema.sql`+`02_seed.sql`): normalized `currencies` lookup, `markets` precision cols, `exchange_markets` `*_rebase` cols (feed raw-pipeline job 3) + **`our_profit_percent`/`slippage_percent`/`buy_sell_commission_percent` (2026-08-25, flat % per exchange+market, defaults 0.1/1/0.35) for [[project_adjustment]]**; ⚠ local seed stale vs server DB (server has 8 exchanges incl. ramzinex/bitget/bybit/ompfinex/okx, subscribed rows); ⚠ new columns need a manual `ALTER TABLE` on any already-provisioned DB; ⚠ `02_seed.sql`'s INSERT header briefly went out of sync with its row values (broken sed, self-caught same day)
- [Kafka Topic Strategy](project_kafka_topic_strategy.md) — ID-based topics: raw ingest `ex{exchange_id}-raw` (2d, lowered from 7d 2026-08-24), per-exchange+pair stages `ex{id}-p{id}-{stage}-flink` (1h), dead-letter `ex{id}-p{id}-rejected-flink` (2d, lowered from 7d 2026-08-24), aggregated output `p{pair_id}-{side}` (6h), key=null, all pre-provisioned by warmup.sh; **⚠ there is NO `ex{exchange_id}-p{pair_id}-{side}` input topic — that naming was wrong here until 2026-08-10 and had already leaked into the staleness exporter, see [[staleness-exporter]]**; **normalizer stage topics `ex{id}-p{id}-{stage}-flink` (2026-07-19) created FIRST in warmup.sh because sources read `latest`, shared `rejected-flink` dead-letter at 2d**; **`scripts/purge-topics.sh` (2026-08-19) empties every pipeline topic via `kafka-delete-records` without touching topics/configs/registry — the counterpart to warmup.sh and a scalpel next to `refresh-normalizer`'s `down -v`; ⚠ it does NOT reset Flink's keyed state, and it holds a THIRD copy of `NORMALIZER_STAGES`**; **`e2e/topics/topics.go` carries a FOURTH copy of these retention constants (comment: "as in scripts/warmup.sh") — updated in lockstep 2026-08-24, but nothing enforces that going forward**;
- [Avro Schemas](project_avro_schema.md) — canonical schemas/subjects: `aggregated-order-book-event` TRUE Confluent Avro wire format for the terminal aggregator + web; raw-pipeline trio `raw-order-book-event` (nullable asks/bids for per-side snapshots, nullable sequence*id, jump 0 = snapshot feed) + `order-book-snapshot` + `rejected-order-book-event`, all registered via warmup.sh; display-only fields rule; price/qty as strings; \*\*per-step latency timings (2026-07-15): ONE `pipeline_timings: PipelineTimings` record (named `{stage}*{in,out}`nullable millis, NOT an array) on all raw-pipeline schemas; total end-to-end =`book_build_out − event_time`**; **`sequence_jump_tolerance` added 2026-08-22 to `raw-order-book-event` + its nested copy in `rejected-order-book-event` (`default: 0` ⇒ BACKWARD-compatible; only ex5 sets it, to 10)**
- [Order Book Aggregation](project_orderbook_aggregation.md) — business rules: union-never-sum across exchanges, sort asks↑/bids↓ tie-break qty desc, snapshot/update semantics, strict `sequence_id`/`sequence_jump` gap handling (reused by raw-pipeline job 2); no Flink checkpointing configured (cold-start deferred)
- [Order Book Web UI](project_orderbook_web.md) — standalone Go app (`web/`, hexagonal `internal/` packages) consuming `p{pair_id}-{side}` Avro topics, id→display enrichment from postgres, live WebSocket order book; vendored deps, dockerized; **2026-08-10 per-exchange view — one page, pair + exchange dropdowns, "All exchanges" = job 6's union, any exchange = job 5's `ex{id}-p{id}-orderbook-snapshot-flink` ([[book-builder]]); the two-sides-in-one-record vs one-record-per-side mismatch is absorbed entirely in the decoder (`Decode` → `[]RawBook`, dispatch on Avro FULL NAME not topic, record-level exchange/simulation pushed down onto levels), so nothing downstream sees two shapes; three user decisions — TWO consumers (agg earliest, per-exchange LATEST, because `ConsumeResetOffset` is client-wide and those topics are a full book per event), hub now pushes each client ONLY its selected pair+exchange (`select` message, `ServeWS`'s read loop finally means something), and both dropdowns come from a postgres-derived `catalog` message since filtering broke inferring them from the data; hub `latest` is keyed by `{pair,exchange,side}` not by topic**; **2026-08-24 merged view — a THIRD dropdown entry (`All exchanges (merged)`) for [[price-merger]]'s `p{id}-{side}-merged`, sentinel `exchange_id = -1` so `Selection`/hub/`WSSelect` stayed untouched; **`exchange_id` is ONE ubiquitous language with no bare numbers — `1+` = a real exchange, `domain.AggregatedExchangeID` = 0, `domain.MergedExchangeID` = -1, one const block in `internal/domain/book.go` mirrored as `AGGREGATED`/`MERGED` in `public/index.html` (the JS can't import Go, so the two must move together)**; `Book.Merged` (not the nil `Exchange`, which is nil for the union too) is what `ExchangeID()` and therefore the `latest` key branch on; ONE consumer regex widened to `^p[0-9]+-(asks|bids)(-merged)?$` — the deliberate mirror image of `MergerJob`'s, which must exclude `-merged` or it eats its own output; per-level `exchange_ids[]` resolved to a LIST (`Level.Exchanges`, comma-joined in the UI) and the scalar `Level.Exchange` left zero so merged rows don't all read `unknown`**
- [Normalizer Scaffold](project_normalizer_scaffold.md) — M1 DONE 2026-07-15: `flink/normalizer/` Maven multi-module (`normalizer-parent` + `normalizer-common`, job modules added per-milestone); common = models/serdes/AvroSchemaLoader/RefreshingLookup/Decimals (18 tests); parameterized run-job.sh (manifest Main-Class); `docker-compose.yml` = full stack, one 8-slot Flink cluster for the 5 upstream normalizer jobs + the terminal aggregator; **normalizer SMOKE RULE (2026-07-15): feed RAW to `ex{id}-raw` only, run the whole chain, capture the job-under-test's topic, event_time = execution time so per-stage pipeline_timings are verified live**
- [Pair Extractor](project_pair_extractor.md) — M2 DONE 2026-07-15: `job-pair-extractor` (raw pipeline job 1) — **9 parsers (ex1–ex9) since 2026-08-26 — every seeded exchange now has one, so `dropped-no-parser` fires for nothing in the DB** (scope lives in `Parsers.byExchangeId()`); **ex7 ompfinex LANDED 2026-08-24 by a teammate, resolving the 2026-07-14 postponement — REST snapshot + Centrifugo WS delta, but a TRUE delta feed (ex6/ex8 family), so "Centrifugo ⇒ ex1-style null-seq bootstrap" is no longer a safe inference; the first DYNAMIC `sequence_jump` (`u - U` per message, i.e. continuity is `U_n == seq_{n-1}`, NOT Binance's `+1`), and the first parser to MIX event-time clocks (REST = `data.time` epoch MICROseconds ÷ 1000, WS = processing time); ⚠ its REST snapshot is SEQUENCED by `data.lastUpdateId` — the OPPOSITE of the ex6 call — which is safe only if that counter is the WS `u` counter, evidenced by just TWO consecutive samples and NOT by a mid-stream resync, so the ex5 loop is still an open risk; ⚠ both side keys REQUIRED (missing ⇒ whole message silently dropped) unlike ex6/ex8's null side; ⚠ no `OmpfinexParserTest`, no `ex7-*.json` fixtures, absent from `SimulationFlagTest`/`RecordIdTest`, and `sample-raw-data.md` § ex7 still holds NO captures**; **ex1 nobitex REVISED 2026-07-21 — NOT snapshot-only: NiFi publishes a REST snapshot (`action:"snapshot"`+injected `pair` field → type snapshot/null seq) AND WS deltas (Centrifugo, no action → type update/`seq=pub.offset`/jump 1); coupled job-2 resync + deploy-together warning, see [[type-validator]]**; **ex2 bitpin REVISED 2026-07-25 — the identical split (REST `action`+injected `pair`=`BTC_USDT`; WS `orderbook:{market}` → update/jump 1); ⚠ `event_time` has TWO wire types in ex2 — REST = epoch-millis NUMBER, WS = ISO-8601 STRING, same field name; NO job-2 change (ex1's `baselinePending`+`out_of_order` guard are exchange-agnostic); ex1/ex2 REST payloads are now identical except the timestamp field NAME (`lastUpdate` vs `event_time`), so neither snapshot branch may key off `action` alone; unit-verified, not live**; **⚠ ex1/ex2 REVISED AGAIN 2026-09-02 — the "WS = delta" call above was itself wrong: fresh live captures show WS pushes are full snapshots (silent deletes, no qty=0 marker), so both are now REST-snapshot + WS-snapshot, each independently ordered by its own counter with NO gap detection, and job2/job5 needed ZERO code changes (the snapshot branch already supported this shape via ex4/ex5); `baselinePending` now dead for both (joins ex3/ex9); e2e scenarios renamed + recomputed, `ControlEx1*` scenarios deleted — see the dated section at the end of this file**; **ex9 lbank LANDED 2026-08-26** (parser `977e770` by a teammate 2026-08-25; javadoc/tests/fixtures/samples/e2e followed 2026-08-26) — the platform's SECOND snapshot-only feed after ex3, and the first that is snapshot-only AND event-time-ordered for real; **NO sequence field exists anywhere on the wire, so `sequence_id=null`/jump 0 by user decision — `TS`-as-sequence was REJECTED to avoid ex5's cadence trap** — which means ex9 can reach exactly ONE reject reason (`out_of_order`) and can emit NO control command EVER; ⚠ `TS` is the platform's only non-epoch-millis timestamp (zone-less ISO-8601 local date-time) read as **UTC** — user-confirmed but unverifiable from the payload, and an 8 h error would surface only as a staleness alarm; ⚠ equal timestamps are ACCEPTED (guard is `<`, not `<=`) — deliberate, and shared with ex3/ex1/ex2 so it must never be tightened for ex9 alone; ⚠ frames are selected by SHAPE not by `type` (all captures say `fdepth`), so a future lbank channel carrying `pair`+`TS`+`depth` would be misread as a book; both side keys REQUIRED (a half-frame would wipe a side on a snapshot feed); market key is lowercase-underscore `btc_usdt` — the case trap the 2026-07-20 note predicted, pinned twice; NO job-2 change needed, the null-seq guard built for ex1 was inherited whole; **VERIFIED — 269 Java tests green AND e2e scenarios 55–59 all PASS LIVE 2026-08-26** (run without `stack.Provision`; the trimmed local seed WAS the ex9 slice, so no seed patch needed — contrast ex7's first run); ⚠ still no live MUTATION check, so the suite is proven to pass but not proven to bite; `latest` offsets, per-exchange event_time stamping (ex3/ex4 = processing time), wire level order passed through, postgres driver shaded into job jar; **ex4 ramzinex RE-VERIFIED 2026-08-22 against a 2nd live capture (`orderbook:11`) — parser output correct end-to-end and the risky BOTH-SIDES-DESCENDING assumption (best ask LAST) re-confirmed; ⚠ market `11` missing from the local seed and, at rial-scale prices, needs `price_amount_rebase=-1` on the server**; **ex5 bitget REVISED 2026-08-22 — new wire sample: channel `books50` → grouped `depth`/`scale`, `action` gained `"update"` (delta feed, qty "0" = delete), `seq`+`pseq` GONE off the wire (the new `checksum` is a CRC, not a sequence) → sequence id is now the inner string `ts` with jump 600 and the first-ever nonzero `sequence_jump_tolerance` (10); one-sided updates now null the absent side like okx**; **ex5 gained a SECOND stream 2026-08-23 — bitget's REST depth snapshot, the same REST+WS split ex1/ex2 have: NiFi injects `pair` and tags `action:"snapshot"`, so `action` CANNOT discriminate; the parser branches on the shape of `data` (OBJECT = REST, ARRAY = WS), sides are `a`/`b`, levels are JSON NUMBERS (reuses ex4's `fromNumericArrays`); ⚠ user chose to sequence it by its own `data.ts` at 600 ± 10 like a WS snapshot, NOT the null-seq `baselinePending` bootstrap — so the first WS update after a resync must land 590–610 ms after the REST book's ts or it gaps (todo.md); NO job-2 change needed**; **ex6 bybit gained a SECOND stream 2026-08-24 — the REST `/v5/market/orderbook` snapshot, the same REST+WS split ex1/ex2/ex5 have; unlike all three the discriminator is EASY (book under `result` vs `data`, and the WS frame has no `action` at all), market key = `result.s` (so both ex6 streams derive the key the same way; injected `pair` is redundant here), string levels on BOTH streams (no numeric hazard, unlike ex5), event time = `result.cts` (same matching-engine field the WS branch reads); ⚠ **null-seq/jump 0 — `result.u` is NOT on the WS counter and the proof is ARITHMETIC not statistical: the REST capture is 24.3 h LATER than the WS pair yet its `u` is 171,928,550 LOWER (38992362 vs 210920912), and a monotonic counter cannot run backwards** — adopting it re-creates the ex5 resync loop (here as an instant `stale_or_duplicate`, since the REST value is the smaller one); the two WS captures needed NO change (u jump 1, seq jump 10, `cts`) and the capture settled one shape question — an unchanged side on a live delta is a present-but-EMPTY array `"b": []`, harmless ONLY because job 5 clears a side just when type==snapshot, so null and empty differ only on snapshots; NO job-2 change**; repeatable e2e smoke test `smoke-pair-extractor.sh` (all 10 fixtures green, deterministic via pre-produce end-offset read); 2 runtime gotchas fixed 2026-07-15 → `ExchangeMarketsLoader` needs explicit `Class.forName` postgres driver load (Flink child-first classloader vs DriverManager), and registry `raw-order-book-event` had to be re-registered to v2 with `pipeline_timings` or the Avro sink NPEs
- [Type Validator](project_type_validator.md) — M3 DONE 2026-07-15: `job-type-validator` (raw pipeline job 2) — keyed `(exchange_id,pair_id)` `KeyedProcessFunction` `{lastSeq, awaitingSnapshot, baselinePending}`; **⚠ 2026-09-02 — ex1/ex2 are NOT delta feeds after all (see [[project_pair_extractor]]); zero code changes needed here, since the snapshot branch already supported a sequenced-but-never-jump-checked snapshot (ex4/ex5's shape) — ex1/ex2's WS side just needed to become one; `baselinePending` now dead for ex1/ex2 (joins ex3/ex9); ex6 is the current live example of the null-seq-REST-resync bootstrap the notes below call "ex1"**; **2026-07-21 ex1 resync: a null-seq snapshot (ex1 REST / ex3) sets `baselinePending`, the first `update` after it adopts its offset as the baseline — exchange-agnostic, ex6/ex8 `no_baseline` unchanged**; discriminator is `type`+`sequence_id==null` NOT jump alone (delta snapshots also carry jump>0); **null-seq frames (ex3, ex1 REST) have no seq → ordered by EVENT TIME: an older-than-last-accepted one rejects `out_of_order` before re-arming baseline (2026-07-21 fix — old REST snapshot replayed after WS deltas must not overwrite the newer book)**; snapshot = out-of-order drop, update = gap/jump rule (`no_baseline`/`awaiting_snapshot`/`stale_or_duplicate`/`sequence_gap`); valid → `-type-validated-raw-flink`, rejects → dead-letter `-rejected-flink` side output; **2026-07-21 gap-drop: true-gap branch ALSO emits a synthetic `type="reset"` on the main stream (still dead-letters the update) → job 5 empties the book → aggregator drops the exchange**; **LIVE BUG 2026-07-22: `type` is an Avro ENUM not a free string — the `"reset"` symbol NPE'd the serializer, fixed by adding it to `Type` in `raw_order_book_event.avsc` + re-registering (v2/id 7); resubmit jobs to re-fetch; gap-drop is exchange-agnostic → redo only the LIVE test per delta feed ex1/ex6/ex8**; **2026-08-22 contiguity is now a WINDOW `last + jump ± sequence_jump_tolerance` (new schema field, `default: 0` = the old exact check for every exchange but ex5's 600 ± 10); ⚠ the window guards EVERY transition incl. snapshot→update by explicit user decision, so bitget's real +22 ms post-snapshot burst is an expected `sequence_gap` — flagged risk, see [[raw-pipeline-decision]] and todo.md**; **⚠ 2026-08-23 LIVE FIX — the ex5 resync loop, diagnosed on the dev server and both causes in job 1, not job 2**: `control-plane` was nothing but `snapshot_request/sequence_gap/ex5/p1`; measured 4569 `ex5-raw` frames / 36 min (BTCUSDT, the only subscribed ex5 market) showed (a) the WS `depth` channel sends **NO snapshots at all** (3538 updates, 0) so the REST body is ex5's ONLY baseline, (b) the REST `data.ts` is a different clock — behind the last WS update **57%** of the time, and the next update landed in `600 ± 10` only **9.9%** of the time, so ~90% of resyncs gapped instantly and looped, and (c) update→update is bimodal (575–625 **plus a real 725–775 cluster**), only 93.2% inside `600 ± 10`. Fix = `BitgetParser` stamps the REST snapshot **null-seq/jump 0** (the ex1/ex2 `baselinePending` bootstrap, so the two clocks never meet) and the WS window widened to **650 ± 110** = [540,760] = 99.83% of live transitions. Replaying the capture: **1030 book resets + 1031 requests (28.6/min) → 4 + 5 (0.1/min)**; verify with [[project_type_validator]]**; **2026-08-23 AUDIT — "a snapshot is ORDERED, never jump-checked", every exchange (user-stated invariant): job 2 is the platform's ONLY sequence validator and `getSequenceJump()` appears in ONE expression, inside the update branch, so the code already conformed and NOTHING changed in production; what was missing was proof — every pre-existing snapshot test used jump 0 (where a leaked check is invisible) or hit `lastSeq == null` / `resyncPending()`, so 4 tests on NONZERO jumps were added (48 → 52) and 2 mutations confirm they bite; gap detection has exactly TWO legal sites, snapshot → next update and update → update, which means the ex5 +22 ms open risk is NOT narrowed by this**; 48 harness tests + smoke 3/3; had to re-register stale `rejected-order-book-event` to v2 (pipeline_timings NPE, same as job 1); smoke gotchas = avro producer `use.logical.type.converters`, newly-discovered topic seeks earliest
- [Rebaser](project_rebaser.md) — M4 DONE 2026-07-18: `job-rebaser` (raw pipeline job 3) — stateless UNKEYED `ProcessFunction`, `price × 10^price_amount_rebase` / `quantity × 10^volume_amount_rebase` exact via `scaleByPowerOfTen`; exponents from `RefreshingLookup` keyed `{exchange_id}|{market_id}` (own `RebaseFactorsLoader`, NOT job 1's market-string loader); **missing row → dead-letter `no_rebase_row` (user decision — this is why it's a ProcessFunction not a RichMapFunction)**; null side stays null (ex3 half-book); smoke temporarily UPDATEs the rebase row + waits out the lookup refresh + restores in an EXIT trap (seeded row is 0/0 = identity, proves nothing); 9 tests + smoke 3/3
- [Precision](project_precision.md) — M5 DONE 2026-07-18: `job-precision` (raw pipeline job 4) — stateless UNKEYED `RichMapFunction`, truncates DOWN to `markets.{price,quantity}_precision` via `RefreshingLookup` keyed by `pair_id` ALONE (precision is a property of the market, not the exchange); **nonzero quantity truncating to 0 → EMIT "0" and KEEP the level (user decision, revising an earlier drop decision) — job 5 reads that as a level-delete, which is intended: size below the lot precision is not representable liquidity; flooring was rejected as inventing size**; **2026-07-20 FIX — prices that COLLIDE after truncation (1.234 and 1.235 both → 1.23) are MERGED into one level with their quantities SUMMED, raw sum truncated once (not truncate-then-sum), same rule on snapshot and update; previously both were emitted and job 5's price-keyed MapState silently kept only the last, losing liquidity — so level count in NO LONGER equals level count out**; **NO dead-letter — null/missing precision is a passthrough, deliberately unlike job 3** (un-truncated is correct-but-verbose, un-rebased is silently corrupt); nullable cols need `rs.getObject` not `rs.getInt`; 21 tests green, smoke extended for the merge but NOT re-run live since (no DB mutation — seeded 2/8 is already non-identity)
- [Book Builder](project_book_builder.md) — M6 DONE 2026-07-18: `job-book-builder` (raw pipeline job 5) — keyed `(exchange_id,pair_id)` `KeyedProcessFunction`, one `MapState` per side (canonicalized price → quantity), emits the FULL book (`order-book-snapshot`) on every event; **snapshot vs update differ ONLY by "clear the side first"**, so `quantity == 0` is a delete in both — and those deletes also come from job 4's dust truncation, not only from exchanges (commented at the delete branch); **`null` side ≠ empty side — THE ex3/wallex per-side snapshot merge point**; sides sorted asks↑/bids↓ because MapState order is undefined; no seq re-validation, no dead-letter (job 2 already validated); cold-start = empty book until the next snapshot (no checkpointing, platform-wide gap); **registered `order-book-snapshot` was stale v1 without `pipeline_timings` — re-registered, same trap as jobs 1/2**; 15 tests + smoke 3/3 (3 ordered cases on ONE accumulating book)
- [Aggregator](project_aggregator.md) — terminal `job-aggregator` (raw pipeline job 6) consuming job 5's full books directly, unioning across exchanges per `(pair_id,side)` → frozen `p{id}-{side}`, subject **`aggregated-order-book-event`** (TRUE Confluent Avro; web decoder resolves by wire-id, maps by field name); consumes job 5's `order-book-snapshot` full books directly so a `type="reset"` ([[type-validator]]) that empties an exchange's book drops it from the union; `SnapshotSplitter` → `CrossExchangeAggregator` (union-never-sum, empty book ⇒ exchange drops out) → inline sink; models/serializer `AggregatedOrderBook`/`AggregatedLevel`/`AggregatedOrderBookSerializer`; deployed via Makefile `refresh-normalizer` (submitted downstream-first; stateful, so resetting it means recycling the job); ([[type-validator]] reset marker + [[book-builder]] reset branch feed it); 12 tests green; **`smoke-aggregator.sh` (2026-07-22) — raw ex8→whole chain→p1-{side}, asserts ex8 by exchange_id, 4 cases incl. gap⇒reset⇒ex8 drops⇒resync returns; syntax-only, not run live**
- [Adjustment](project_adjustment.md) — `flink/adjustment/` (2026-08-24, user request, built in steps) standalone single-module Flink project mirroring [[price-merger]]'s shape, reading job 6's `p{id}-{side}` → `p{id}-{side}-adjusted`; **three stages — `BuySellCommission` 0.35% → `OurProfit` 0.1% → `Slippage` 1% — each sizing its amount off the price the level ARRIVED with (`AdjustedLevel.basePrice`, not on the wire), so they ADD to asks × 1.0145; they COMPOUNDED to × 1.014548535 for one day until the user corrected it, and the correction means the chain ORDER no longer affects the result at all (addition commutes)**; each stage writes the rate it applied onto the event, which is why the output got its OWN subject `adjusted-order-book-event` (steps 1–2 reused the aggregated one; job 6's frozen web contract must not grow fields for a downstream job) — ⚠ `warmup.sh` needed a `register_schema` line, and a missing one there is INVISIBLE to a green e2e run; **⚠ the sign convention (asks BUY → price up, bids SELL → price down) lives in ONE method `Prices.multiplier` and was ASSUMED, never confirmed — inverting it fails 7 tests**; exact BigDecimal throughout, `movePointLeft(2)` not `divide(100)`, and NOTHING is rounded to tick size because picking a rounding direction is a money decision; **2026-08-25 ALL THREE rates DONE end to end, including commission** — flat % per `(exchange_id, market_id)` (DB columns in [[project_db_schema]]), read via a `RefreshingLookup` each in `BuySellCommissionFunction`/`OurProfitFunction`/`SlippageFunction`, mirroring `RebaseFactorsLoader`; user asked for commission the "exactly same job" as profit/slippage, same day. All three forced OFF the record and ONTO `AdjustedLevel` (breaking Avro change, the record now carries ZERO rate fields) since one book unions levels from multiple exchanges and a record-wide rate can't represent per-exchange rates; `Prices.applyPercent` (record-wide) deleted once nothing called it any more, `applyPerLevelPercent` is the only arithmetic method left; falls back to the old hardcoded constant (0.35/0.1/1) when a level's exchange+pair has no exchange_markets row (no dead-letter output in this job); 23/23 tests green, shaded jar zero flink/avro entries but the postgres driver IS bundled (compile scope, not provided — see the deploy-failure note); **✅ VERIFIED LIVE 2026-08-25 via `make run-all-jobs`, `orderbook-adjustment` RUNNING with no exceptions**; a separate slot-leak infra bug surfaced during that verification, see [[flink-deploy-tooling]] (unrelated to this job's code)**; output subject `adjusted-order-book-event` since step 3 (no longer reuses the aggregated one); ⚠ the model mirrors the schema in FULL because this job RE-ENCODES — a missing field is silently replaced by its schema default (3 mutations pin it); ⚠ anchored regex or it eats its own output AND the merger's; ⚠ **slot budget is 8/8 — the cluster is full**; OPEN: the record carries job 6's `id` through rather than minting one, which is what "no changes" means but conflicts with [[record-lineage]]
- [Price Merger](project_price_merger.md) — `flink/merger/` (2026-08-11, user request) — standalone single-module Flink project **outside `flink/normalizer/`**, reads job 6's `p{id}-{side}` and SUMS equal prices across exchanges onto `p{id}-{side}-merged`, subject `merged-order-book-event`; a **parallel view, not a replacement** — [[orderbook-aggregation]]'s union-never-sum and [[aggregator]] are untouched; **stateless** (one aggregated record already IS the whole book for that pair+side, so no MapState/splitter/reset handling — that is the whole payoff of reading job 6 rather than job 5); grouped by **(price, simulation)** so simulated depth is never summed into live; level carries `exchange_ids[]` + `source_ids[]` positionally aligned; **lineage bends one rule** — level `source_ids` name job-5 snapshots (one hop too far, because an aggregated LEVEL has no id), record-level `source_id` is the strict single parent; self-contained (duplicates `AvroSchemaLoader`+`Decimals.canonicalize` rather than build-order-coupling to `normalizer-common`); ⚠ input regex is **anchored** or the job eats its own output; output price is canonicalized so it may differ from job 6's string; same Flink image/cluster, slots now 7/8; 16 tests green, **NOT run live**; deploy via `make run-all-jobs` ([[flink-deploy-tooling]]), **web done 2026-08-24** ([[orderbook-web]]'s third dropdown entry, `exchange_id = -1`), still NOT wired into e2e or the staleness exporter
- [Flink Deploy Tooling](project_flink_deploy_tooling.md) — **one** `flink/run-job.sh <job>` for every project under `flink/` (2026-08-11, replaced the two per-project copies + their Makefiles); discovers jobs by scanning poms for a shade `<mainClass>` — no hardcoded list, so `common` is auto-excluded and new jobs need no script edit; handles both layouts (single-module `merger` = plain `package`; aggregator-pom `normalizer` = `-pl <mod> -am`); ⚠ empty-arg guard is load-bearing (`"$project/$want"` with `want=""` collapses to the project dir and silently built `merger`); `flink/Makefile` now takes **`JOB=`** not `MODULE=`; root Makefile keeps order in `NORMALIZER_JOBS`/`ALL_JOBS` vars and **`make run-all-jobs`** is the only target that submits the merger — `refresh-normalizer` is still normalizer-only, so the merger is down after a refresh; **2026-08-18 `flink/run-local.sh <job>` runs a job in-process on a MiniCluster — no Flink image, no cluster, no submission (discovery shared with run-job.sh via `job-discovery.sh`); cost was two pom holes that only appear off-cluster, since `provided` is neither shaded nor transitive — `flink-connector-base` (comes in with flink-dist on the cluster) and the fact that a module resolves only the slice of `/opt/flink/lib` its own pom names, so the normalizer parent now declares the image's lib set for every module**; **2026-08-19 native VS Code debugging: `flink-clients` moved `test` → `provided` in both poms (the comments already said the cluster supplies it; identical to shade, but only `provided` is on the classpath the IDE hands a `src/main` class — `test` made F5 die with "No ExecutorFactory found"), and `.vscode/launch.json` has one `flink: <job>` launch config per job with env from `.vscode/flink-local.env` (keep in step with run-local.sh's exports). `DEBUG=1 ./run-local.sh <job>` + the attach config survive as the fallback**; **2026-08-25 `make run-all-jobs` slot leak — `NoResourceAvailableException` on every job despite `numberOfTaskSlots=8` matching 8 jobs; JobManager held stale slot bookkeeping (`freeSlots:0` with only 2 jobs RUNNING); `taskmanager`-only restart insufficient, needed `taskmanager` + `jobmanager` both restarted; unrelated to any job's code — check `freeSlots` vs actual RUNNING count before assuming a regression**
- [Flink Production Hardening](project_flink_production.md) — production review of the compose Flink cluster (2026-08-26, **scope agreed 2026-08-29; the round is COMPLETE — M1/M7/M2a/M2b/S5/M4/M3/S4/M9/S7 applied on `docs/flink-production-hardening`, none deployed or observed**). **⚠ 2026-08-31 the hardening MOVED OUT of `docker-compose.yml` into a separate `docker-compose.prod.yml`; `docker-compose.yml` was restored byte-for-byte to `f023a47` (the last commit before M1) and is the DEV stack** — every config block quoted in the doc now lives in the prod file. Standalone copy, not a `-f a -f b` override, because **an override cannot remove a service** and prod must drop dev's single `taskmanager` for `taskmanager-1..4`; the cost is that **a service added to one file must be added to the other by hand**. NiFi is in the prod file byte-identical (out of scope means do not modify, not remove); volume/project names are identical across both so an existing box keeps its data; `run-job.sh` now **discovers** TM containers instead of listing them; Makefile gained `prod-up`/`prod-deploy`/`prod-verify`/`prod-logs` with **no `down -v`, no `git pull`, and unconditional `--build`**, closing **H1**, **H3** and half of **H2**. **⚠ Checkpointing was UN-POSTPONED on `feat/checkpointing` (merged 2026-08-31)** — enabled per job in `CheckpointingConfigurer` (10 s interval, EXACTLY_ONCE, `filesystem` storage at `file:///opt/flink/checkpoints`, RETAIN_ON_CANCELLATION, restart strategy deliberately left to the cluster config). **The risk below was accepted, not mitigated** — nothing coordinates checkpoints across the 8 job graphs, so it is now live rather than avoided. Wiring cost: a shared `data-collector-flink-checkpoints` volume on the JobManager and every TaskManager **in both compose files** (`filesystem` storage means the JM writes metadata and the TMs write state files, so it must be one directory — per-container copies recover from an empty dir), plus `/opt/flink/checkpoints` pre-created and chowned in the Dockerfile alongside ha/archive. Note this also **un-inerts M4**: `DeliveryGuarantee` is still `NONE` on every sink, but `AT_LEAST_ONCE` would now actually do something. The original reasoning, still true: Flink has no cross-job checkpoint coordination, so a crashed upstream job replays into a downstream job that never restarted and **silently reverts price levels** — verified, `BookBuildFunction` reads `sequence_id` only to stamp output (line 93) and job 2, the only validator, is four stages upstream. This **corrected an earlier claim in the same doc** that the book builder is "idempotent by construction" — idempotent is not commutative. User's call: restart from `latest` and re-baseline from a snapshot rather than resume from possibly-wrong state, so **S1 is CLOSED** (keep `latest()`); M5/S2/M6/S6/L4 were parked with the postponement and are now **reopened by implication, none of them done**. **NiFi is out of scope** (not ours, dev-only in compose) — do not propose changes to it. Agreed for now, in apply order: **M1** explicit `restart-strategy` (the `disable` default applies only because checkpointing is off — biggest win, no dependency on it), **M7** `unless-stopped` + logging, **M2a/M2b** memory (image default `1728m` ⇒ ~384 MB task heap for all 7 jobs) then 4 TMs × 2 slots, **S5** TM healthcheck, **M4** producer `acks=all` + `enable.idempotence=true` (⚠ `AT_LEAST_ONCE` is **inert** without checkpointing — it flushes *on checkpoint*), ~~**M8** bind 7070 privately~~ **DROPPED 2026-08-31** — `docker-compose.yml` runs on 5+ dev/test envs and must stay **host-agnostic**: a literal `192.168.150.104` makes the container fail to start on any other host, and loopback-only breaks reaching the UI from a laptop. **Binds M3 and M9 too** — no hard-coded host addresses or scrape targets. The REST API still has **no auth**; that control belongs to a firewall or a per-host override file, **M3** ZooKeeper HA (applied 2026-08-31; HA keys go on the **TaskManagers too**, not just the JM as the report shows — leader fencing rejects a TM left on `NONE` — and it creates two new hazards: `make run-all-jobs` after an HA recovery gives **16 jobs**, and recovery restores all 8 graphs in **no order** while `latest()` sources need downstream-first), **S4** HistoryServer (applied 2026-08-31; host port **7071**, no `depends_on` on the JM by design, and **only terminal jobs are archived** — a job stuck restarting never appears), **M9** Prometheus + Alertmanager + Grafana (applied 2026-08-31; config in `monitoring/`, host ports **9090/9093/3001** — 3000 is `web`. **Metric names came from a live scrape, not docs**: a throwaway JM+TM running the bundled `TopSpeedWindowing`, then a real Prometheus against it — all 10 rules `health: "ok"`, two observed firing. Traps that scrape exposed: Flink exposes **everything as a `gauge`** including `numRestarts`, so `increase()` warns but works; TaskManager `host` labels are the **container IP with dots as underscores** and change on every recreate, so group on `instance`; `numRestarts` is **JobManager-scope**. Deviations: added `FlinkJobManagerDown` because a dead JM is the quietest failure of the set — `numRunningJobs != 8` cannot fire when the series is gone; split restarts into warning + loop because M1 turned dying jobs into permanently-restarting ones that stay `RUNNING` and never reach S4's HistoryServer; used `backPressuredTimeMsPerSecond` over the `isBackPressured` gauge, which a 15 s scrape mostly misses. **Two gaps: `records_lag_max` is the one unverified name** — if wrong the rule is *silently dead*, confirm with `curl -s localhost:9250/metrics | grep -i records_lag_max` — and **Alertmanager delivers nowhere** until a receiver is filled in, no channel having been agreed. `amtool check-config` was never run, no image locally; `prom/alertmanager` and `grafana/grafana` **both need a pull**), **S7** pin images (applied 2026-08-31; `redis_exporter:v1.86.0`/`kafka-exporter:v1.9.0` **read off the cached images**, `kafka-ui:v0.7.2` is the one unverified pin but fails loudly at `up`; digests were rejected because the cached `RepoDigests` equal the local image IDs and would not resolve elsewhere. SHA goes in the jar manifest **and** the upload filename — the manifest is durable but needs the jar, the upload name is on the cluster's `/jars` but is lost on a JM restart). **Volume-ownership trap, found 2026-08-31 and verified by probe:** Docker creates a missing mount destination `root:root` and the Flink image runs as **uid 9999**, so `/opt/flink/ha` (M3) and `/opt/flink/archive` (S4) — neither of which exists in the image — came up unwritable and silently broke HA. Fixed by a `mkdir`+`chown` stanza in `flink/normalizer/Dockerfile`, so **the next deploy must `up -d --build`**; `/opt/flink/log` was fine only because it already exists in the image. Traps: never set `env.java.opts.all` (image uses it for the Java 21 `--add-opens` list); container memory limit must exceed `process.size`; **`make refresh-normalizer`'s `docker compose down -v` would wipe Kafka/Postgres volumes on a prod box**. **PR #11 review 2026-09-02**: the `read_committed` fix had stopped at the `flink/normalizer` module boundary — `flink/merger`, `flink/adjustment`, `web/internal/kafka/consumer.go` and `e2e/consumer/consumer.go` all still read uncommitted from now-transactional topics; **fixed** (a sink turning transactional is a change to every consumer of that topic, in every language). **CORRECTION: `lpa-staleness-exporter` is NOT broken by this** — `kafka-python==2.0.2` never filters control batches (`is_control_batch` defined, referenced nowhere), so its `seek(end-1)` still returns the commit marker and its timestamp; ⚠ **upgrading kafka-python silently breaks it**. Verified so nobody re-derives them: the 3 checkpoint metric names are real in the 2.2.0 jar; `CheckpointingOptions.CHECKPOINT_STORAGE` was a **no-op** where it was set — **fixed**, storage now goes through `env.configure()` (which `addAll`s every key) instead of `getCheckpointConfig().configure()` (which ignores that key); ⚠ 2.x keys are `execution.checkpointing.storage`/`.dir`, not the 1.x `state.*` names; broker transaction settings are already correct; all 9 transactional-ID prefixes unique and correctly numbered. **Open, undecided: EXACTLY_ONCE adds ~30s avg/60s worst end-to-end latency across 6 hops** (vs sub-second), below which `output_threshold_seconds: 10` no longer makes sense; **Also fixed 2026-09-02**: `tolerableCheckpointFailureNumber` 0 → 3 (one failed checkpoint no longer kills a job — the 2026-08-31 loop's mechanism; 3 stale "tolerable is 0" comments updated with it) and `RETAIN_ON_CANCELLATION` → `DELETE_ON_CANCELLATION` (retention was unusable — `run-job.sh` never passes `savepointPath` — and stranded 8 checkpoint dirs per deploy; governs cancellation only, failures still restore). `amtool check-config` run for the first time: SUCCESS. Still open: no disk alert on the checkpoint volume. Full config blocks + the three routes back to checkpointing in the doc; ordered checklist in `todo.md`; see [[flink-deploy-tooling]], [[project_type_validator]], [[project_book_builder]]
- [Raw Pipeline Decision](project_raw_pipeline_decision.md) — FINAL 2026-07-13: NiFi publishes verbatim raw payloads to `ex{id}-raw`; 6 chained Flink jobs (pair-extract → type-validate → rebase → precision → book-build → aggregator) in ONE Maven multi-module project; accuracy > latency; raw samples RESET then RE-CAPTURED 2026-07-14 for ALL in-scope exchanges into `sample-raw-data.md` (3 regimes confirmed; job-2: gap/jump rules on delta feeds ex6 bybit `u`/jump-1 + ex8 okx `ts`/jump-300 — **plus ex1 (2026-07-21) and ex2 (2026-07-25), both re-classified from snapshot-only to REST-snapshot+WS-delta, jump 1 — ⚠ REVERSED AGAIN 2026-09-02, both are back to full-snapshot-every-msg on BOTH streams, see [[project_pair_extractor]]**; remaining snapshot feeds ex4 `pub.offset` + ex5 `seq` (+ ex1/ex2 WS again) get an OUT-OF-ORDER drop check only — ex3 wallex has no ordering field, no protection; non-book frames just discarded; ex1 always single-doc; remaining lead: bybit qty-"0" delete frame); **ex7/ompfinex POSTPONED 2026-07-14 → SUPERSEDED 2026-08-24, scope is now ex1–ex8**; **per-step latency tracking REQUIRED 2026-07-15 (named `PipelineTimings` record, ingest+emit per job, total = book_build_out − event_time)**; tasks in todo.md
- [BigDecimal Rules](project_bigdecimal_rules.md) — prices/quantities are `BigDecimal`-from-wire-string, never double; scale-sensitive equals → TreeMap or canonicalized string keys
- [TDD Workflow](project_tdd_workflow.md) — team is test-first; existing code gets spec-based (not characterization) coverage; Flink operator test harness stack; JaCoCo-on-JDK26 noise is non-fatal
- [E2E Harness](project_e2e_harness.md) — `e2e/`; every scenario asserts job 5's snapshot stream AND job 6's final aggregated book (all 41 now spell `WantAggregated` out; a nil one is derived from the last wanted snapshot, which only holds while a scenario feeds ONE exchange); also the `-serve` HTTP mode (`POST /scenarios/run`, chi, one run at a time, failed scenario = 200 + `"status":"failed"`) and its swaggo docs (`/swagger/`, build `e2e/` with `-mod=mod` — never `go mod vendor`, regenerate `e2e/docs/` with `swag init` after struct changes; the Try-it-out example is injected at serve time from `scenario.Ex1PrecisionDust`, not in the committed spec); **ex5 block RENUMBERED twice — 2026-08-22 (grew 5→6) and again 2026-08-23 (→7); CURRENT: ex5 25–31, bybit 32–37, okx 38–43, control plane 44 and 46 (45 and 47 RETIRED 2026-09-02 — `ControlEx1NoBaselineThenGap`/`ControlEx1LaggingRestResync` deleted, their ex1-delta premise no longer reachable, see [[project_pair_extractor]]); grep the Go identifiers rather than quoting numbers; **01–14 (ex1/ex2) renamed 2026-09-02, WS pushes are snapshots not deltas — see the dated section in project_e2e_harness.md**; `27-ex5-jump-tolerance` is the only scenario exercising `sequence_jump_tolerance`, and `31-ex5-rest-snapshot-resync` (new 2026-08-23) is the only one feeding bitget's REST wire shape — verified live AND live-mutation-checked (shift the REST `data.ts` −100 ms → record 4 comes back an empty book)**; **scenario 48 `48-ex6-rest-snapshot-resync` added 2026-08-24 — bybit's REST `result` envelope, APPENDED to the tail rather than slotted into the bybit block 32–37 (renumbering is the recurring tax here; grep identifiers, not numbers); it is the ex5-loop regression test ported to ex6's counter, and the load-bearing assertion is the COUNT — ONE `sequence_gap` reject and ONE `snapshot_request`, a second of either means the REST `u` leaked into job 2's state; source 04 carries the real captured u=38992362 against a WS feed at 8xx so a wrong impl fails in EITHER direction, and source 02 is the suite's only present-but-EMPTY side on an UPDATE; ⚠ NOT RUN LIVE — builds/vets/gofmts clean but the docker stack was down, so the snapshot COUNT and the reset record's event time are reasoned from Ex6SequenceGap/Ex5RestSnapshotResync, not observed**; **ex7/ompfinex scenarios 49–54 added 2026-08-24 by a teammate (`data_ex7.go`, mirrors ex6's six) — **ALL SIX VERIFIED LIVE 2026-08-24, 6/6 PASS**; ⚠ a run done WITHOUT `stack.Provision` fails all six with `got 0 records` because provisioning is what loads `02_seed.sql` — check `exchange_markets` before believing an all-zero result; ⚠ SLOTTED IN rather than appended, so 44–48 shifted to 50–54 and the comment above the control-plane pair still says "46/47" while the entries say 52/53; every ex7 scenario sets `IgnoreEventTime` (deltas carry no wire timestamp), which also blanks the REST snapshot's, so the parser's MICROseconds ÷ 1000 conversion is asserted NOWHERE; missing a stale/duplicate case and a rebase case — the header's reason for skipping rebase ("no ompfinex market with non-zero rebase factors") is FALSE, `02_seed.sql` gives ~half the ex7 rows `price_amount_rebase=-1`**; **logging is `log/slog` since 2026-08-08 (`-log-level`, default info) — Info = scenario start/PASS/FAIL + process milestones, Debug = everything per-scenario; the real noise was never the log lines but the per-scenario `mvn` and the `docker compose` output, which now buffer and replay only on failure**
- [Simulation Flag](project_simulation_flag.md) — `simulation` int added 2026-08-03 across all 4 schemas + all 6 jobs + e2e + web; 0/absent = live, 1 = simulation, other values undefined and carried verbatim; **TWO wire carriers — a root FIELD for the eight object-root exchanges (ex3 is the only array root), a TRAILING THIRD ARRAY ELEMENT `{"simulation":N}` for ex3/wallex** (whose root is an array; 2-element frames still parse as 0, 4+ dropped); **per-LEVEL on the aggregated topics** (`exchange_id, simulation, price, quantity`) because job 6 unions across exchanges; job 2's synthetic `reset` marker explicitly inherits it, job 5 stamps last-event-wins and keeps it OUT of MapState; ⚠ re-register all 4 subjects or the Avro sink throws (same trap as `pipeline_timings`); every e2e example is `simulation: 1`; web plumbs it to the WebSocket but does NOT render it
- [Record Lineage](project_record_lineage.md) — `id`/`source_ids` added 2026-08-03 across all 4 schemas + all 6 jobs + e2e + web (**field was `sink_id` until the 2026-08-04 rename — pure rename, same logic**); **`id` is RE-MINTED at every hop, it is NOT an end-to-end correlation id despite the name** (7 different ids across the pipeline; walk the chain hop by hop) and `source_ids` is IMMEDIATE parents only; same two wire carriers as [[simulation-flag]] (root field / ex3's trailing object at **index 2**, never a 4th element); **⚠ job 1 DROPS a payload with no id → deploy NiFi's change BEFORE the jars or a feed loses 100% of its data (`dropped-no-id`)**; job 5 is the only fan-in (every event still holding a resting level + always the triggering one → MapState value became `RestingLevel{price,quantity,id}`); **2026-08-08 job 5's snapshot levels each carry a per-level `source_id` = the job-4 event that last SET that price — without it a trace of ONE price dead-ended at "one of these N events", since a record-level set has no mapping back to prices — and the snapshot's record-level `source_ids` array was REPLACED by a single `trigger_id` (it had become `{trigger} ∪ {level owners}`, so it added only the trigger, and only positionally); ⚠ `trigger_id` is NOT always among the level ids — a delete-only event owns none and a reset empties the book — which is exactly why it is its own field; job 6 deliberately does NOT propagate it (a level names the snapshot, the snapshot's level of that price names the event — each hop one step); the snapshot `PriceLevel` is now the ONLY one with the field and `serde/PriceLevels` decides from the SCHEMA, and `order-book-snapshot` is stale AGAIN**; **2026-08-08 e2e `events` realigned field-for-field to the schemas — `last_sequence_id` + `AggregatedSide.event_time` were missing, and the timings model was goavro-WIRE-shaped and wrong 3 ways (union branches keyed by FULL name; logical-type unions keyed `long.timestamp-millis`; value is a number) — see [[e2e-harness]]; all 4 `_example.json` are hand-maintained docs nothing validates, verified 2026-08-08**; job 6 = per-record `id` + per-LEVEL `source_id` (no record-level array); jobs 3/4 are no longer no-ops unlike simulation; dead-letter envelope and its nested event have SEPARATE lineages; ⚠ re-register all 4 subjects; ⚠ Avro returns `Utf8` not `String`; **e2e asymmetry — the 177 SOURCE payloads each spell a literal `id` out (2026-08-04) since they are inputs, and `stampID` now only FILLS ONE IN rather than overwriting (blank id = hard error, duplicate-key trap); every id further down is job-minted and random per run, so e2e checks those structurally then strips them**
- [Scope Discipline](project_scope_discipline.md) — **HARD RULE, user 2026-07-27: do ONLY what was asked, and CONFIRM any extra work before doing it.** "Initialize X" ≠ X plus the first task that uses X; a `todo.md` milestone is a backlog, not permission to implement it; "we will provide Y" means WAIT for Y, don't reconstruct it. Written after a session's unrequested M13-phase-1 code was thrown away
- [Control Plane](project_control_plane.md) — merged to main 2026-08-18: **⚠ 2026-09-02 — can no longer engage for ex1/ex2 at all (their WS pushes are snapshots now, not deltas — see [[project_pair_extractor]]); `ControlEx1NoBaselineThenGap`/`ControlEx1LaggingRestResync` e2e scenarios DELETED, ex6 is the live example of the null-seq-REST-resync shape**; job 2 puts a `snapshot_request` on a shared topic `control-plane` asking NiFi to re-send a book it can no longer trust — the return path for the `type="reset"` that [[type-validator]] opened; **once per EPISODE not per rejected event** (`snapshotRequested` ValueState, cleared by the three resync branches), triggers are `no_baseline`+`sequence_gap` ONLY (not stale_or_duplicate/out_of_order); Avro on subject `control-command` (was plain JSON until 2026-08-18), carrying `simulation` CARRIED from the gap event and `id`/`source_ids` **DERIVED not inherited** — one gap event parents three records (reset marker, dead-letter, command); ⚠ **`scripts/warmup.sh` registers subjects by name while e2e registers every `schemas/*.avsc`, so a green e2e run proves nothing about `make warmup`**; e2e `want_control_commands` asserted on every scenario where **nil means "nothing was requested", never "skip"** + 2 feature-grouped scenarios in `data_control.go`, re-verified live 2026-08-18; **2026-08-19 DEADLOCK FIXED — a resync snapshot that was REJECTED (ex1/ex2 REST clock skew → `out_of_order`, or ex6/ex8 `seq <= lastSeq`) left both flags set forever, and since `lastEventTime` only advances inside `emit()` the guard could never afterwards be satisfied: no further command was EVER sent and the market stayed dark until restart. Both guards now sit behind `!resyncOutstanding()`** — covered live 2026-08-22 by scenarios **44/45**, the first in the suite to feed a resync the guards would have THROWN AWAY, both mutation-checked against the real stack (4 snapshots vs 7, 5 vs 8, 1 command vs 2); **2026-08-22 `reason` added to the command** (user request) — `no_baseline` | `sequence_gap`, Avro `default: ""` so v2 is a BACKWARD-compatible evolution, derived from `lastSeq == null` so it costs no state, and a RETRY carries the reason that OPENED the episode rather than the `awaiting_snapshot` its trigger was dead-lettered with; ⚠ **re-register the subject AND resubmit job 2, in that order** — the serializer caches the write schema on first use; **2026-08-22 REDESIGNED after the user asked for a rethink from before the feature existed** — `awaitingSnapshot`+`snapshotRequested`+`pendingSimulation`+`pendingSourceId` collapse into ONE `ValueState<Long> resyncRequestedAt` (value = when we last asked, nullness = book untrustworthy; reject reason DERIVED from `lastSeq == null`), and re-asking is driven by the rejected events themselves rather than a timer, so `onTimer`/`scheduleRetry`/the key parse are gone and **the feature costs ZERO net state** (4 ValueState fields, the same four as before the control plane existed). Deliberate gap: a market that goes SILENT after a gap gets no re-ask. 43 unit tests, 5 mutations, all 45 e2e green; ⚠ the NiFi consumer does not exist in this repo, cold start asks for every market at once, and the name collides with the market-subscription HTTP API
- [Staleness Exporter](project_staleness_exporter.md) — `lpa-staleness-exporter/` derives its watch list from `exchange_markets` and **must mirror `scripts/warmup.sh` names or it reports phantom topics as `stale=1`**; `NORMALIZER_STAGES` is duplicated in both files with nothing enforcing it; dead-letter deliberately unmonitored (inverted semantics); serial poll loop has a known scaling ceiling; **`topic_source: both|db|config` picks the source (`db` ignores the manual `topics:` block entirely) and 5 staleness-*episode* series (stale-since / last-recovery / last-duration + 2 counters) are in-memory and accurate only to ±1 poll interval**; **episodes ALSO persist to `topic_staleness_episodes` (2026-08-11, self-installing table absent from `01_schema.sql`, written in EVERY `topic_source` mode) under 3 sharp invariants — `extract(epoch)::float8` or PG18 hands back a `Decimal` that silently breaks recovery, a UNIQUE partial index because two open rows is unrecoverable, and dropping a watched topic must close its episode**; metric consumers (`KafkaTopicStale`) live outside this repo, except the hand-imported drift-prone dashboard copy `grafana-dashboard.json`
- [Market Subscriptions](project_market_subscriptions.md) — `market-subscriptions/` (was `markets/`) Go operator console, one binary with the UI `go:embed`ded, mirroring [[orderbook-web]]'s layout; reads `exchange_markets`+`exchanges` and drives NiFi's two `/control-plane` subscribe/unsubscribe endpoints; **THE invariant: it writes ONLY `pending-subscribe`/`pending-unsubscribe` and NiFi settles the row to `subscribe`/`unsubscribe` — a pending row means NiFi has not followed through, and `Action.Pending()` can never return a settled status**; a failed POST rolls the pending write back (⚠ a TIMED-OUT request may still have reached NiFi); ENUM column needs an explicit `::subscription_status` cast; all 9 settings in `.env` with bad values falling back rather than failing startup; the old CSV CLI is kept at `market-subscriptions/csv-bulk-sync/`; ⚠ built+tested but NEVER run against a live postgres or NiFi
- [NiFi over HTTPS](project_nifi_https.md) — NiFi HTTPS single-user on 8443; raw-IP access fails `400 Invalid SNI` → fix via `NIFI_WEB_PROXY_HOST`
- [Server Build Env](project_ubuntu_server_env.md) — deploy server needed Temurin 21 tarball + apt maven (`scripts/install-deps.sh`); repo root-owned so use `sudo make refresh-normalizer`
- [NiFi Hot Path](project_nifi_hot_path.md) — 2026-08-31 ingest stall at ~100 msg/s: Jolt recompiled its spec per FlowFile (EL `${uuid}` + Transform Cache Size 1) and Redis was hit once per message for a constant; replaced by `SimpleDatabaseLookupService`+`LookupAttribute` and a `ReplaceText` prepend, Run Duration 25ms, across all 9 groups; ⚠ `unmatched`⇒`ex-raw` and a non-matching regex ⇒ silent `dropped-no-id`
