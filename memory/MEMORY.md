# Memory Index

- [DB Schema](project_db_schema.md) — postgres `markets` db (`01_schema.sql`+`02_seed.sql`): normalized `currencies` lookup, `markets` precision cols, `exchange_markets` `*_rebase` cols (feed raw-pipeline job 3); ⚠ local seed stale vs server DB (server has 8 exchanges incl. ramzinex/bitget/bybit/ompfinex/okx, subscribed rows)
- [Kafka Topic Strategy](project_kafka_topic_strategy.md) — ID-based topics: input `ex{exchange_id}-p{pair_id}-{side}`, output `p{pair_id}-{side}`, key=null, pre-provisioned by warmup.sh, retention 1h/6h; orderbook-job + fake-data-generator still on the OLD side-first naming; tried-and-reverted experiments + kafka-ui AVRO serde config gotchas
- [Avro Schemas](project_avro_schema.md) — the 3 canonical schemas/subjects (`orderbook-event` doc-only/JSON for orderbook-job; `price-level-event` + `consolidated-order-book-event` TRUE Confluent Avro wire format for consolidator + web); display-only fields rule; price/qty as strings
- [Order Book Aggregation](project_orderbook_aggregation.md) — business rules: union-never-sum across exchanges, sort asks↑/bids↓ tie-break qty desc, snapshot/update semantics, strict `sequence_id`/`sequence_jump` gap handling (reused by raw-pipeline job 2); no Flink checkpointing configured (cold-start deferred)
- [Order Book Consolidator Decision](project_orderbook_consolidator_decision.md) — FINAL: greenfield `flink/orderbook-consolidator/` as Java Flink DataStream job; scope R1/R2/R4/R5/R6, R3 (stale_time expiry) POSTPONED; two-stage keyed topology (HARD RULE); built + 25 tests green; schema from registry ONLY at runtime; compose volumes/restart policy; open risk: NiFi input format unverified
- [Order Book Web UI](project_orderbook_web.md) — standalone Go app (`web/`, hexagonal `internal/` packages) consuming `p{pair_id}-{side}` Avro topics, id→display enrichment from postgres, live WebSocket order book; vendored deps, dockerized
- [Raw Pipeline Decision](project_raw_pipeline_decision.md) — FINAL 2026-07-13: NiFi publishes verbatim raw payloads to `ex{id}-raw`; 6 chained Flink jobs (pair-extract → type-validate → rebase → precision → orderbook-build → price-level-emit) in ONE Maven multi-module project; accuracy > latency; raw samples RESET then RE-CAPTURED 2026-07-14 for ALL in-scope exchanges into `sample-raw-data.md` (3 regimes confirmed; job-2: gap/jump rules ONLY on delta feeds ex6 bybit `u`/jump-1 + ex8 okx `ts`/jump-300, snapshot feeds get an OUT-OF-ORDER drop check on ex1/2/4 `pub.offset` + ex5 `seq` — ex3 wallex has no ordering field, no protection; non-book frames just discarded; ex1 always single-doc; remaining lead: bybit qty-"0" delete frame); **ex7/ompfinex POSTPONED — scope is ex1–ex6 + ex8**; tasks in todo.md
- [BigDecimal Rules](project_bigdecimal_rules.md) — prices/quantities are `BigDecimal`-from-wire-string, never double; scale-sensitive equals → TreeMap or canonicalized string keys
- [TDD Workflow](project_tdd_workflow.md) — team is test-first; existing code gets spec-based (not characterization) coverage; Flink operator test harness stack; JaCoCo-on-JDK26 noise is non-fatal
- [Fake Data Generator](project_fake_data_generator.md) — Go dev tool standing in for NiFi; STALE vs current pipeline (old topic naming + JSON), only feeds old orderbook-job until updated
- [NiFi over HTTPS](project_nifi_https.md) — NiFi HTTPS single-user on 8443; raw-IP access fails `400 Invalid SNI` → fix via `NIFI_WEB_PROXY_HOST`
- [Server Build Env](project_ubuntu_server_env.md) — deploy server needed Temurin 21 tarball + apt maven (`scripts/install-deps.sh`); repo root-owned so use `sudo make refresh-consolidator`
